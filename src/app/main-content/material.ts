export class Material {
    name: string;
    url: string;
    count: number;

    constructor(
        name: string,
        url: string,
        count: number
    ) {
        this.name = name;
        this.url = url;
        this.count = count;
    }

}

// domain model database create materials from here
