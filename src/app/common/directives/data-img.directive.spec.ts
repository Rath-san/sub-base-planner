import { DataImgDirective } from './data-img.directive';

describe('DataImgDirective', () => {
  it('should create an instance', () => {
    const directive = new DataImgDirective();
    expect(directive).toBeTruthy();
  });
});
